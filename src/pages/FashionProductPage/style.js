import { Col } from "antd";
import styled from "styled-components";

// chỉnh sửa card trong trang type
export const WrapperProducts = styled.div`
    display: flex;

    gap: 12px;
    margin-top: 5px;
    flex-wrap: wrap;
`
export const WrapperNavbar = styled(Col)`
    background: #fff;
    margin-right: 10px;
    padding: 10px;
    border-radius: 4px;
    height: fit-content;
    margin-top: 5px;
    width: 200px;

`